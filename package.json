{
  "name": "monorepo_space",
  "version": "1.0.0",
  "private": true,
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "changeset:add": "npx changeset add",
    "changeset:version": "npx changeset version",
    "changeset:publish": "npx changeset publish",
    "release": "npx changeset version && npx changeset publish && git push --follow-tags",
    "release:tag": "npm run changeset:version && git push origin main && git tag v$(date +'%Y%m%d-%H%M%S') && git push --tags"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@changesets/cli": "^2.29.5",
    "@types/node": "^24.2.1",
    "prettier-plugin-organize-imports": "^4.2.0",
    "prettier-plugin-packagejson": "^2.5.19",
    "typescript": "^5.9.2"
  }
}
